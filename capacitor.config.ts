import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ingenia tus Finanzas',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
